# Frontend environment variables
# Copy to .env.local for local development overrides

# Optional: override the API base URL (defaults to /api/v1 which works via
# the Vite proxy locally and Vercel routing in production)
# VITE_API_URL=/api/v1
